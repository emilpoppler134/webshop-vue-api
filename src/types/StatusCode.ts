export enum SuccessCode {
  OK = 200,
  CREATED = 201,
  NO_CONTENT = 204,
}

export enum ErrorCode {
  BAD_REQUEST = 400,
  UNAUTHORIZED = 401,
  FORBIDDEN = 403,
  NO_RESULT = 404,
  CONFLICT = 409,
  SERVER_ERROR = 500,
}
